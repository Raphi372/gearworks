{
  "name": "gearworks",
  "version": "1.0.0",
  "description": "Factorio-inspired browser factory game with server-authoritative deterministic-lockstep multiplayer.",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "gearworks-server": "server/server.js"
  },
  "scripts": {
    "start": "node server/server.js",
    "start:prod": "NODE_ENV=production node server/server.js",
    "dev": "node server/server.js --port 8080",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop gearworks",
    "pm2:logs": "pm2 logs gearworks",
    "validate": "node scripts/validate.js",
    "test": "node scripts/test.js",
    "test:integration": "node --test test/*.test.js",
    "test:all": "node scripts/test.js && node --test test/*.test.js",
    "build:client": "node scripts/build-client.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "optionalDependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Raphi372/gearworks"
  }
}
