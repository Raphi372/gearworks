# Cloudflare Pages redirects. The client is a single-page app; unknown paths
# fall through to the custom 404 (Pages serves /404.html automatically on 404).
# Add SPA-style rewrites here if deep-linking routes are introduced later.
